{
  "name": "keystone-site",
  "version": "1.0.3",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build": "npm run css && npm run js && cp index.html 404.html fonts.css dist/",
    "css": "tailwindcss -c tailwind-config.js -i input.css -o dist/styles.css --minify",
    "js": "esbuild main.js --bundle --minify --outfile=dist/app.js",
    "test": "node --test",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^5.1.0",
    "helmet": "^7.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "eslint": "^8.0.0",
    "tailwindcss": "^3.4.0"
  }
}
